# Вложенные свойства

```javascript
const myCity = {
  city: "New York",
  // Вложенный обьект
  info: {
    isPopular: true,
    country: "USA",
  },
};

// Точечная запись
console.log(myCity.info.isPopular);

// Скобочная запись
delete myCity.info["isPopular"];

console.log(myCity);
```
