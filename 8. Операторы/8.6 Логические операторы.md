# Логические операторы

`!` - Оператор НЕ, всегда возвращает значения типа boolean

`&&` - Оператор И, возвращает значение одного из операндов

`||` - Оператор ИЛИ, возвращает значение одного из операндов
