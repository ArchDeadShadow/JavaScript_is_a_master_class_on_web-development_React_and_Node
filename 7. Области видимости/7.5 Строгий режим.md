# Строгий режим

```javascript
"use strict";
```

```javascript
"use strict"; // Эта строка должна быть первой в глобальной области видимости или в области видимости функции

function myFn() {
  a = true; // Запрещает использование необьявленных переменных
  console.log(a);
}

myFn();

console.log(a);
```
