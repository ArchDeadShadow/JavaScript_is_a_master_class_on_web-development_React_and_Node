# Корневой элемент в JSX

JSX - Должен иметь один корневой элемент

Валидный JSX:

```jsx
return (
  <div>
    <h1>JSX в React</h1>
    <p>JSX похож на HTML</p>
    <Footer text={text} />
  </div>
);
```

Невалидный JSX:

```jsx
returm (
	<h1>JSX в React</h1>
	<p>JSX похож на HTML</p>
)
```
